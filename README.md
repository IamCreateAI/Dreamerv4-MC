# VisionaryWorld
autoregressive model for world model research, including training, inference and distributed system


# install
conda create -n dreamer python=3.12
pip install torch==2.9.0
pip install av
MAX_JOBS=16 pip install flash-attn --no-build-isolation

